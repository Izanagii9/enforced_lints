name: enforced_lints
description: Configurable lint rules enforced across Dart/Flutter projects via the analyzer plugin API.
version: 0.1.3
repository: https://github.com/Izanagii9/enforced_lints
issue_tracker: https://github.com/Izanagii9/enforced_lints/issues

topics:
  - linting
  - analyzer
  - dart

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  analyzer: ">=8.0.0 <13.0.0"

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows: